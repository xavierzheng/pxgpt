# Needed by smoke.py and bench.sh only. The vLLM server runs in Docker and
# needs nothing from here.
#
# Ceiling matches the repo root's requirements.txt: these scripts drive the local
# vLLM server through the same openai SDK, so an untested major breaks them too.
# The FLOOR is deliberately lower than the root's 1.66.0 -- smoke.py and bench.sh
# use only chat.completions and models.list, never responses.create.
openai>=1.40.0,<=3.0.0
jsonschema>=4.20.0
